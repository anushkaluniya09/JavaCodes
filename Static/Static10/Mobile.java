package com.Static.Static10;

class Mobile {
    static class SIM {
        String operator;
        SIM(String operator) { this.operator = operator; }
        void showSIM() { System.out.println("SIM Operator: " + operator); }
    }
}
